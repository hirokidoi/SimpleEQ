import Foundation

/// ドライバが申告しうるサンプルレートの集合。レートに依存する導出が、どのレートでも成り立つことを
/// 確かめる検証がこの列を使う。
///
/// 写しを置かずドライバのソースから直接読む形は採らない。レートに依存する検証すべてがパースに
/// 依存し、書式の変更が無関係な検証まで落とすため。
enum TestSampleRates {
    static let all: [Double] = [44100, 48000, 88200, 96000]
}
